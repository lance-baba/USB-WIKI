# 安全策略

## 威胁模型（请先了解这个项目是什么）

Wiki-USB 是一个**单机、本地优先**的便携知识库：

- HTTP 服务**只监听 `127.0.0.1`**，不对外提供服务，没有多用户与权限体系。
- 没有云端组件；数据库是本地 SQLite 文件，和笔记一起放在 U 盘上。
- 可选的外部连接只有两类：本地 Ollama（`127.0.0.1:11434`），以及你**自行配置**的云端
  AI API（只有在你填了 `api_key` 时才会用到）。

因此这里**不存在**传统 Web 服务的常见漏洞面（越权、SQL 注入、会话劫持等）——
没有会话，也没有远端用户。但下面几类问题是真实存在的。

## 一、值得关注的风险

### 1. 抓取的第三方网页是不可信内容

剪藏功能会把网页原文留存为 HTML 用于「原版预览」。这些 HTML 是**第三方内容**，
已被刻意隔离：

- 预览时用 `<iframe sandbox="">` 加载 —— **不授予任何权限**，
  脚本、表单、弹窗、顶层跳转全部被禁用。

如果你发现任何绕过该沙箱的路径（例如能执行脚本、能访问父页面），**请按安全问题上报**。

### 2. 剪藏内容的间接提示注入

抓来的网页正文会进入检索结果，进而进入送给 LLM 的提示词。恶意网页可以因此
包含「忽略之前的指令」之类的内容来影响模型输出。

这**不是本项目的代码缺陷**（任何 RAG 系统都面临它），但值得知晓：
不要对完全不可信的来源无条件信任 AI 的回答。系统会在回答中标注引用出处，便于你核对。

### 3. 云端 API Key 的存放

`config.ini` 里的 `api_key` 是**明文存储在 U 盘上**的。

- 不要把高额度、可用于支付的 Key 写进去。
- `config.ini` 已在 `.gitignore` 中，不会被提交。
- **不要把 U 盘借给不信任的人**，或借出前清空 `api_key`。

### 4. 服务端口

服务绑定 `127.0.0.1`，局域网内其它机器无法访问。请勿改成 `0.0.0.0` ——
那样等于把一个无鉴权的知识库完整暴露给同网段所有人。

## 二、上报漏洞

请**不要**通过公开 issue 上报安全问题。

优先使用 GitHub 的
[私密漏洞上报](https://docs.github.com/code-security/security-advisories/guidance-on-reporting-and-writing-information-about-vulnerabilities/privately-reporting-a-security-vulnerability)
功能（仓库 → Security → Report a vulnerability）。

如果该功能不可用，请开一个**不含利用细节**的 issue 说明「有安全问题需要私下沟通」，
维护者会主动联系你。

### 请包含

- 受影响的版本 / 提交
- 复现步骤或最小 PoC
- 影响范围（例如「能让剪藏预览执行任意脚本」）
- 你的环境（OS / Python 版本 / 浏览器）

### 响应预期

这是个人维护的开源项目，无法承诺 SLA。通常会在**数天内**给出初步回应。
修复后会在 CHANGELOG 中记录（在不暴露利用细节的前提下），并致谢上报者（如你愿意署名）。

## 三、明确不在范围内的

- 物理接触攻击（拿到 U 盘本身就能读到全部内容——这是设计前提，不是漏洞）
- 你自行填写的第三方 API 服务端的行为
- 你自己改配置把服务暴露到公网后遭受的攻击
- 扫描件 / 图片 / 音视频的 OCR / 转写（本项目不含该能力）
